{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A command-line application that dynamically generates a README for any GitHub project using inputs form a user.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ryan A Williams",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "inquirer": "^6.3.1"
  }
}
